<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "SD Test Application", secret_codes_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <% if current_user.first.present? && current_user.first.present? %>
            <li><%= link_to "#{current_user.first.capitalize} #{current_user.last.capitalize} (#{current_user.email})", edit_user_registration_path, class: "label" %></li>
          <% else %>
             <li><%= link_to current_user.email, edit_user_registration_path, class: "label" %></li>
          <% end %>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
          <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>